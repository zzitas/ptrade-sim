# 备选标题(任选其一)

1. 《别再排队等恒生回测了:我把 PTrade 搬进了本地 Python》
2. 《开源|ptrade-sim:一个能防未来函数的 PTrade 本地回测环境》
3. 《从聚宽迁到 PTrade?这个开源工具帮你把策略"搬"过去,还能本地秒级回测》

> 公众号摘要(120 字内):恒生 PTrade 回测排队久、跑得慢、调试难?开源项目 ptrade-sim 把 PTrade 回测引擎搬进本地 Python:事件模型与撮合语义对齐、内置防未来函数设计与聚宽策略转换器,通达信数据秒级回测。MIT 协议,开箱即用。

---

# 别再排队等恒生回测了:我把 PTrade 搬进了本地 Python

## 引子:一次等了三分钟的数据扫描

做过 PTrade 量化的人都有过这种体验——

写好策略,上传到恒生 PTrade 交易端回测,然后盯着日志看它一行一行地爬:

```
[开盘前相关性分析] 执行完成 - 耗时: 182793.66ms
⚠️ 执行耗时过长: 182793.66ms
```

**182 秒,三分钟,只完成了开盘前的一个数据扫描。** 原因很简单:策略要对全市场 765 只 ETF 逐只查询历史行情,而每一次 `get_history` 都是一次独立的数据服务调用。一个回测日下来,光是取数就是几千次远程调用。

改一行代码 → 上传 → 排队 → 跑三分钟 → 看日志 → 再改。这个循环转上一天,人就废了。

于是有了这个项目:**ptrade-sim**,把 PTrade 的回测引擎"搬"进本地 Python。

👉 开源地址:**github.com/zzitas/ptrade-sim**(MIT 协议)

【截图:GUI 主界面】

---

## 一、它是什么

ptrade-sim 是一个**本地 PTrade 模拟/回测环境**,纯 Python 实现,核心目标是两句话:

1. **在本地复现恒生 PTrade 的回测语义**,让策略在模拟器里怎么跑,上真机就怎么跑;
2. **把聚宽策略一键转换成 PTrade 原生策略**,生成的文件可以直接上传恒生交易端回测。

它不是一个通用的回测框架(backtrader、vnpy 那样),而是一个**专精 PTrade 生态的迁移与调试工具**——如果你正从聚宽迁移到券商 PTrade,或者每天被真机回测的速度折磨,它就是为你准备的。

---

## 二、核心功能

### 1. 事件模型与撮合语义,逐项对齐 PTrade

| 语义项 | ptrade-sim 的实现 |
|---|---|
| 事件框架 | `initialize / before_trading_start / handle_data / after_trading_end` + `run_daily` 定时任务 |
| 成交量限制 | 委托不超过当根 bar 成交量的 25%,超量自动调整并记日志 |
| T+1 | 当日买入冻结,次日可用(`enable_amount`) |
| 停牌 | 拒单、快照 `trade_status=HALT` |
| 交易日历 | 真实 A 股日历 |
| 价格口径 | 前复权 |
| 全市场列表 | 自动获取,且**只含回测起点前已上市**的 ETF(避免未来函数) |

策略代码里用的 `get_history / get_etf_list / get_Ashares / order_target_value / context.blotter.current_dt`,全部按 PTrade 的行为实现,不用改一行代码就能跑。

### 2. 三种数据源,按需切换

- **通达信本地终端**:只要电脑上开着通达信,行情直接本地读取,回测秒开;
- **网络行情**:腾讯主源 + 东财备用,前复权,自动增量更新与缓存;
- **合成行情**:离线演示用,零依赖跑通全流程。

### 3. 图形界面,每次回测自动落盘完整日志

Tkinter 写的 GUI(不引入任何重型依赖):选策略、选区间、选资金、选数据源,点运行。实时日志、持仓表、成交表、净值曲线一屏展示。

更实用的是:**每一次回测自动保存完整日志**到 `logs/时间戳_策略名_区间.log`,包括 GUI 里被刷掉的 DEBUG 明细。策略为什么那天没调仓?打开当天的日志文件,一目了然。

【截图:回测日志文件示例】

### 4. 聚宽兼容垫片

`attribute_history / get_price / get_extras('unit_net_value') / get_current_data / finance.run_query / log.set_level`,甚至打新三件套,全部有对应实现。聚宽写的策略,基本原样能跑。

---

## 三、防未来函数:回测可信度的底线

这是本项目最想强调的设计原则。**一个会偷看未来的回测,比没有回测更危险。**

很多日线回测框架的默认行为是:盘中查询"今天的日线"时,直接返回完整的当日 K 线——包括还没走出来的收盘价、最高价、最低价。这意味着策略在早上 10 点就知道今天收盘是多少,回测收益自然漂亮,上实盘立刻原形毕露。

ptrade-sim 做了三件事:

1. **形成中 K 线**:盘中用 `include=True` 查当日日线,返回的是"正在形成"的 K 线——收盘=当前日内价,量按已走时间折算,15:00 之后才变成完整日线;
2. **日内合成价**:盘中报价由"开盘→收盘时间插值 + 微幅确定性噪声"生成,任意时刻的报价都不等于未来值,成交撮合同样如此;
3. **上市日过滤**:全市场列表只包含回测起点前已上市的代码,杜绝"回测 2023 年却买到了 2024 年才上市的 ETF"。

同时我们也诚实地标注了边界:只有日线数据,盘中路径是合成的。**依赖截至昨日数据的信号(动量排名等)与真机一致;依赖盘中瞬时价的信号(网格、量比)是近似**——手册里写得明明白白,不吹"回测=实盘"。

配套了一个防未来函数回归测试(`test_lookahead.py`),每次改引擎都会跑一遍,防止将来改代码时不小心把泄漏改回来。

---

## 四、聚宽 → PTrade 一键转换(附真机踩坑全记录)

这是项目里最"脏活累活"的部分,也是对 PTrade 用户最值钱的部分。

```bash
python jq_to_ptrade.py jq_我的策略.py ptrade_我的策略.py
```

生成的**单个策略文件**内置自包含垫片层,不依赖任何本地模块——直接上传恒生 PTrade 交易端就能回测。转换器处理的不只是 API 改名,而是我们在真机上一条条日志踩出来的差异:

- 恒生把原生 API 注入策略命名空间的方式与聚宽不同,垫片要在正确的时机快照原生函数(处理不好就是 `NameError`);
- `get_stock_name` 在 PTrade 恒返回 `{代码: 名称}` **字典**,聚宽返回字符串——不解包就是对字典切片报 `unhashable type: 'slice'`;
- 原生 `get_price` 要求日期入参是**字符串**,且拒绝 `start_date ≥ 回测日` 的请求;
- 恒生沙箱**禁止 import sys** 等标准库模块,连源码静态扫描都过不了;
- pandas 3.0 下 `series[-1]` 位置取值被移除,垫片用子类恢复兼容。

这些坑,转换器全部处理完毕。**你只管写聚宽风格的策略,剩下的交给它。**

---

## 五、性能优化实战:183 秒 → 3 秒

真机上全池扫描为什么慢?逐只 `attribute_history`,765 只 × 每次几百毫秒的远程调用。

ptrade-sim 的转换器内置了一套批量方案,把真机取数从**每天数千次远程调用压缩到十几次**:

- `get_history(..., security_list=200只列表, field=[多字段], is_dict=True)`:一次调用取回 200 只 × 多字段的全部数据(这是恒生文档里存在、但很少有人用对的形态);
- 分块 + 坏代码自动剔除:某一块批量失败,自动拆成单只重试,坏代码剔除、好代码保留;
- 名称与停牌状态缓存:名称永不变化查一次存进程级缓存,停牌状态按回测日缓存;
- 快照批量预热:原来每只代码 4 次隐藏调用(名称/状态/日线/分钟价),预热后循环内零远程调用。

效果:开盘前相关性分析从 **182,793ms 降到秒级**,全市场播报从"卡死几分钟"到即出表格。

---

## 六、五分钟上手

```bash
# 安装
pip install .        # 或下载 Release 里的发行包

# 图形界面
ptrade-gui

# 命令行回测:策略 开始 结束 资金 数据源(tdx|real|sim)
ptrade-run demo_strategy.py 2026-07-01 2026-08-01 1000000 tdx

# 全市场行情预热(一次性,之后回测不再等网络)
ptrade-prefetch

# 聚宽策略转换
python jq_to_ptrade.py jq_我的策略.py ptrade_我的策略.py
```

要求:Python 3.9+;通达信数据源需要本机运行通达信客户端。

详细安装说明见仓库 `docs/INSTALL.md`,完整语义对照表见 `docs/MANUAL.md`。

---

## 七、适合谁用(以及不适合谁)

**适合:**

- 正在从聚宽/其他平台迁移到券商 PTrade 的量化开发者;
- 想在本地快速迭代策略、真机只做最终验证的人;
- 需要全市场 ETF 扫描、又受不了真机取数速度的策略;
- 想要一个"语义诚实"的回测环境、在乎未来函数的人。

**不适合:**

- 需要真实分钟级撮合的日内高频策略(本项目只有日线,盘中是合成近似);
- 需要 tick 级、滑点建模到极致的研究。

---

## 八、开源与致谢

项目以 **MIT 协议**开源:👉 **github.com/zzitas/ptrade-sim**

欢迎 Star、Fork、提 Issue。尤其是你在真机上遇到的其他 PTrade 兼容性问题——提过来,垫片层会持续吸收这些实战差异。

---

## 免责声明

本项目仅用于学习与研究,不构成任何投资建议。回测结果基于历史数据与合成近似,不代表未来表现;据此实盘操作,风险自负。

---

*如果这篇文章对你有帮助,欢迎点赞、在看、转发给同样在写策略的朋友。*
