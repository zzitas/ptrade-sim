# 真机回测 / 实盘重跑指引(由模拟环境迁移到 PTrade 客户端)

本指引说明如何把在 `ptrade_sim` 模拟环境验证过的策略,迁移到真实 **恒生 PTrade**
客户端运行。模拟环境的核心价值是验证逻辑 / 语法 / 调度无误,真机才使用真实行情与撮合。

## 一、转换:得到自包含的策略文件

模拟环境在 `load_strategy` 时会自动识别聚宽格式(`from jqdata import *` / `.XSHG` /
`.XSHE`),并生成旁路转换文件 `ptrade_<原名>.py`(聚宽接口由内嵌垫片实现)。

```bash
# 在模拟环境侧,加载即触发自动转换(也兼容直接跑聚宽源文件)
python -m ptrade_sim.run jq_七星动态池.py 2026-06-01 2026-08-21 1000000
# 生成的 ptrade_七星动态池.py 即为可直接上真机的自包含文件
```

转换文件头部已内嵌"聚宽→PTrade 兼容垫片":

- 平台在执行策略前会把原生 API 注入模块命名空间;垫片先 `__snapshot__` 一份(`_PT_API`),
  随后同名定义覆盖,`_pt_native()` 优先解析原生实现,保证 `get_history` / `get_price`
  等走平台原生(真机)而非模拟实现。
- 已在 `get_history` / `get_price` 回退中加 `fq="pre"`,与 PTrade 前复权语义一致。
- 已去掉 `import sys` / `__main__` 探测,可通过恒生沙箱静态扫描。

## 二、上真机运行

1. 把 `ptrade_<原名>.py` 上传到 PTrade 客户端策略目录,作为普通 PTrade 策略加载运行。
2. 垫片在真机上**自动识别原生 API**:`get_history` / `get_price` / `get_trade_days` 等
   走平台实现;聚宽专属接口(`attribute_history` / `get_extras` / `finance.run_query` 等)
   走垫片兼容层(尽量不依赖的部分在策略侧有 try/except 兜底)。

## 三、真机健壮性要点(务必保留在策略头部)

- **`is_trade()` 守卫**:模拟环境恒返回 `False`,使"实盘专用"分支(溢价率过滤 / 停牌
  查询 / 费率设置等)与回测一致地跳过;真机返回 `True`,这些分支才会执行。策略中
  区分回测 / 实盘的逻辑都应包在 `if is_trade():` 内。
- **`filter_stock_by_status` 兜底定义**:若目标环境非恒生、无此原生接口,需在策略头部
  以同名 `def` 兜底(哈尔特判定用本地停牌信息),避免 `NameError`。
- **指数成分 / 全 A 列表 / 基本面为本地近似**:模拟环境的 `get_index_stocks` /
  `get_Ashares` / `get_fundamentals` 仅保证选股逻辑可运行;真机请改用平台原生
  `get_index_stocks` / `get_Ashares` 等真实数据。
- **撮合差异**:模拟为市价即时成交 + 25% 成交量上限 + T+1;真机以实际撮合为准,回测时
  二者的成交价 / 滑点会有差异,以真机回测结果为准。

## 四、验证建议

1. 先在模拟环境跑通(`python -m ptrade_sim._selftest ptrade_<原名>.py 起始 结束 real`
   或 `sim`),确认零报错、API 齐备。
2. 真机用**小资金 / 回测模式**先跑同一区间,对比收益曲线与成交笔数是否与模拟环境量级一致。
3. 重点核对:`get_history` / `get_price` 取的字段与复权方式、停牌日行为、`order` 的
   成交量上限是否触达(真机若频繁触发 25% 上限,说明选股池流动性不足)。

> 说明:以上"env A 真机测试通过"指模拟环境侧已验证转换文件语法 / API / 调度无误;
> 真机行情与撮合以券商客户端为准,本指引不替代真机实盘风险自查。
